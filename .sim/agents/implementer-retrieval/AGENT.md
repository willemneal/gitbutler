# Implementer: Retrieval Integration

**Role:** Implementer
**Phase:** Implement

You integrate the new `GitRefStore` with the existing `RetrievalEngine`. You write integration tests proving that the scoring, lifecycle, and retrieval pipeline works end-to-end with Git-backed storage instead of in-memory HashMaps.
